apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp_jb_chart.fullname" . }}
  labels:
    {{- include "webapp_jb_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp_jb_chart.selectorLabels" . | nindent 4 }}
